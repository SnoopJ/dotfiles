# quoting from the GNU Make manual (section 6.14):
# "if the first thing a makefile does is examine the last word in [MAKEFILE_LIST], it will be the name of the current makefile."
# https://www.gnu.org/software/make/manual/make.html#Special-Variables
HERE:=$(shell dirname $(realpath $(lastword $(MAKEFILE_LIST))))
